waerhtrjhjk.lk;l';
Снытко Дмитрий

 
#include <stdio.h>							 
#include <stdlib.h>							
#include <math.h>					

#include <stdio.h>							// библиотека ввода, вывода 
#include <stdlib.h>							// библиотека кирилицы
#include <math.h>							// библиотрека математических функций

int main()												// заголовок функции 
{											//начало
	/*Предусловие: пользователь вводит числа a,b, и c, также вводит делитель k
	  постусловие: программа вычисляет деолителем каких чисел является число a и b или c является число k  */
	system("Chcp 1251");								// переход на кирилицу
	int a, b, c, k;										// обьявление  переменных
	bool flag = 0;
	printf(" Введите a:  ");							// число "a"
	scanf("%i", &a);									// функция ввода числа "a"
	printf(" Введите b:  ");							// число "b"
	scanf("%i", &b);									// функция ввода число "b"
	printf(" Введите c:  ");							// число "c"
	scanf("%i", &c);									// функция ввода число "c"
	printf(" Введите k:  ");							// число "k"
	scanf("%i", &k);									// функция ввода число "k"
	if (a % k == 0)										// если
	{
		flag = 1;
			printf(" a является делителем k \n");
	}	
	if (b % k == 0)
	{
		flag = 1;
		printf(" b является делителем k \n");
	}	
	if (c % k == 0)										// если
	{flag = 1;
	
		printf(" c является делителем k \n");		// если удовлетворает условию ответ выводится
	}
	if (!flag)
		printf(" ни одно из чисел не кратно k  \n");
}											// конец
